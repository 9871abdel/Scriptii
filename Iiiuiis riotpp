-- Roblox LocalScript for Pet Hatch Menu UI

local player = game.Players.LocalPlayer
local screenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
screenGui.Name = "PetHatchGui"
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
screenGui.IgnoreGuiInset = true  -- Ensures UI isn't pushed down by top bar

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 280, 0, 320)
frame.Position = UDim2.new(0, 20, 0, 100)
frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
frame.BorderSizePixel = 0
frame.ZIndex = 10
frame.Parent = screenGui
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 15)

local titleLabel = Instance.new("TextLabel", frame)
titleLabel.Size = UDim2.new(1, 0, 0, 40)
titleLabel.Position = UDim2.new(0, 0, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Pet Hatch Menu"
titleLabel.Font = Enum.Font.FredokaOne
titleLabel.TextSize = 20
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextStrokeTransparency = 0.7
titleLabel.TextScaled = true
titleLabel.ZIndex = 11

local randomizeBtn = Instance.new("TextButton", frame)
randomizeBtn.Size = UDim2.new(0, 240, 0, 40)
randomizeBtn.Position = UDim2.new(0, 20, 0, 50)
randomizeBtn.BackgroundColor3 = Color3.fromRGB(255, 140, 0)
randomizeBtn.Text = "🎲 Randomize Pets"
randomizeBtn.Font = Enum.Font.FredokaOne
randomizeBtn.TextSize = 16
randomizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
randomizeBtn.TextStrokeTransparency = 0.7
randomizeBtn.TextScaled = true
randomizeBtn.ZIndex = 11

local toggleESPBtn = Instance.new("TextButton", frame)
toggleESPBtn.Size = UDim2.new(0, 240, 0, 40)
toggleESPBtn.Position = UDim2.new(0, 20, 0, 100)
toggleESPBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
toggleESPBtn.Text = "Toggle ESP: OFF"
toggleESPBtn.Font = Enum.Font.FredokaOne
toggleESPBtn.TextSize = 16
toggleESPBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleESPBtn.TextStrokeTransparency = 0.7
toggleESPBtn.TextScaled = true
toggleESPBtn.ZIndex = 11

local creditLabel = Instance.new("TextLabel", frame)
creditLabel.Size = UDim2.new(1, 0, 0, 26)
creditLabel.Position = UDim2.new(0, 0, 1, -30)
creditLabel.BackgroundTransparency = 1
creditLabel.Text = "Made by gagscripter4life"
creditLabel.Font = Enum.Font.FredokaOne
creditLabel.TextSize = 14
creditLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
creditLabel.TextStrokeTransparency = 0.7
creditLabel.TextScaled = true
creditLabel.ZIndex = 11

-- Function to randomize pets (placeholder)
local function randomizePets()
    -- Implement pet randomization logic here
end

-- Function to toggle ESP (placeholder)
local function toggleESP()
    -- Implement ESP toggle logic here
end

-- Connect button actions
randomizeBtn.MouseButton1Click:Connect(randomizePets)
toggleESPBtn.MouseButton1Click:Connect(toggleESP)

